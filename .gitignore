/target/
Cargo.lock

# Local Paper test server
/testserver/

# Local config and secret
flow-proxy.toml
forwarding.secret

# Plugin packages and the build cache the proxy creates
/plugins/
fuzz/target/
fuzz/corpus/
fuzz/artifacts/
*.fpkg
*.bat